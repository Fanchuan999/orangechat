# Harness 工作台桥 v1.1

把 Daddy 的操作类需求写入本机 `127.0.0.1:3080` 的固定 `📥 Daddy收件箱` 会话。任务为串行队列，每条带稳定的 `task_id`，便于 Daddy 和代码小屋只展示未结束的任务。

安装 zip 时，请确保压缩包根目录直接包含 `main.js` 与 `manifest.json`。

该插件不传递人设、世界书、Ombre、密钥或完整聊天记录；风险操作仍由 Harness 风险门单独确认。
